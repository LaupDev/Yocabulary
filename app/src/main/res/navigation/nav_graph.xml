<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/wordListFragment">

    <fragment
        android:id="@+id/wordListFragment"
        android:name="com.laupdev.yocabulary.ui.WordListFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_word_list">
        <argument
            android:name="letter"
            app:argType="string" />
        <action
            android:id="@+id/action_wordListFragment_to_addNewWordFragment"
            app:destination="@id/addNewWordFragment" />
        <action
            android:id="@+id/action_wordListFragment_to_wordDetailsFragment"
            app:destination="@id/wordDetailsFragment" />
    </fragment>
    <fragment
        android:id="@+id/wordDetailsFragment"
        android:name="com.laupdev.yocabulary.ui.WordDetailsFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_word_details">
        <argument
            android:name="word_id"
            app:argType="long"
            android:defaultValue="0L" />
        <action
            android:id="@+id/action_wordDetailsFragment_to_addNewWordFragment"
            app:destination="@id/addNewWordFragment" />
        <action
            android:id="@+id/action_wordDetailsFragment_to_wordListFragment"
            app:destination="@id/wordListFragment"
            app:popUpTo="@id/wordListFragment"
            app:popUpToInclusive="true" />
        <argument
            android:name="word_name"
            app:argType="string"
            android:defaultValue="0" />
    </fragment>
    <fragment
        android:id="@+id/addNewWordFragment"
        android:name="com.laupdev.yocabulary.ui.AddNewWordFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_add_new_word">
        <argument
            android:name="word_id"
            app:argType="long"
            android:defaultValue="0L" />
        <action
            android:id="@+id/action_addNewWordFragment_to_wordDetailsFragment"
            app:destination="@id/wordDetailsFragment" />
    </fragment>
</navigation>